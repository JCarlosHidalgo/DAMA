info:
  name: "Auth: Get Student Remain Classes"
  type: http
  seq: 11

http:
  method: GET
  url: "{{API_PATH}}/api/attendance/remain/6ba1c841-834f-4aeb-a285-a500b15638c9"
  auth:
    type: bearer
    token: "{{Client_JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
