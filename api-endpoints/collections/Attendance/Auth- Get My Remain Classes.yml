info:
  name: "Auth: Get My Remain Classes"
  type: http
  seq: 10

http:
  method: GET
  url: "{{API_PATH}}/api/attendance/remain/me"
  auth:
    type: bearer
    token: "{{Student_JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
