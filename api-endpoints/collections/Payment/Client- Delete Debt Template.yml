info:
  name: "Client: Delete Debt Template"
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{API_PATH}}/api/payment/debt-template/00000000-0000-0000-0000-000000000000"
  auth:
    type: bearer
    token: "{{Client_JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
