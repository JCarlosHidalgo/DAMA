info:
  name: "Client: Test Todotix Credential"
  type: http
  seq: 24

http:
  method: POST
  url: "{{API_PATH}}/api/payment/todotix-credential/test"
  auth:
    type: bearer
    token: "{{Client_JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
