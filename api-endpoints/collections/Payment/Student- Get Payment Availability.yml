info:
  name: "Student: Get Payment Availability"
  type: http
  seq: 23

http:
  method: GET
  url: "{{API_PATH}}/api/payment/todotix-credential/availability"
  auth:
    type: bearer
    token: "{{Student_JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
