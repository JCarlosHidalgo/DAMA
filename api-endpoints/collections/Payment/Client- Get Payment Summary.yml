info:
  name: "Client: Get Payment Summary"
  type: http
  seq: 11

http:
  method: GET
  url: "{{API_PATH}}/api/payment/summary"
  auth:
    type: bearer
    token: "{{Client_JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
