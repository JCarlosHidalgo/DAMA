info:
  name: "Student: List Success QR Payments"
  type: http
  seq: 9

http:
  method: GET
  url: "{{API_PATH}}/api/payment/qr/success?Index=0"
  query:
    - name: Index
      value: "0"
  auth:
    type: bearer
    token: "{{Student_JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
