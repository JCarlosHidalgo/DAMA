info:
  name: "Client: Get Subscription QR Debt Status"
  type: http
  seq: 14

http:
  method: GET
  url: "{{API_PATH}}/api/payment/subscription/qr/00000000-0000-0000-0000-000000000000/status"
  auth:
    type: bearer
    token: "{{Client_JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
