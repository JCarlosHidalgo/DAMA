info:
  name: "Client: List Subscription Plans"
  type: http
  seq: 15

http:
  method: GET
  url: "{{API_PATH}}/api/payment/subscription/plans"
  auth:
    type: bearer
    token: "{{Client_JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
