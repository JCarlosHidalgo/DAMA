info:
  name: "Student: List Failed QR Payments"
  type: http
  seq: 10

http:
  method: GET
  url: "{{API_PATH}}/api/payment/qr/failed?Index=0"
  query:
    - name: Index
      value: "0"
  auth:
    type: bearer
    token: "{{Student_JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
