info:
  name: "Client: Update Debt Template"
  type: http
  seq: 4

http:
  method: PUT
  url: "{{API_PATH}}/api/payment/debt-template/00000000-0000-0000-0000-000000000000"
  body:
    type: json
    data: |-
      {
        "Description" : "Paquete 10 clases (actualizado)",
        "ClassQuantity" : 10,
        "Cost" : 550
      }
  auth:
    type: bearer
    token: "{{Client_JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
