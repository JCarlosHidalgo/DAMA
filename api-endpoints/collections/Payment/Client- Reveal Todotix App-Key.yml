info:
  name: "Client: Reveal Todotix App-Key"
  type: http
  seq: 21

http:
  method: GET
  url: "{{API_PATH}}/api/payment/todotix-credential/reveal"
  auth:
    type: bearer
    token: "{{Client_JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
