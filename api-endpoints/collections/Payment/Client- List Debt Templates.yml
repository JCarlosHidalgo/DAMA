info:
  name: "Client: List Debt Templates"
  type: http
  seq: 2

http:
  method: GET
  url: "{{API_PATH}}/api/payment/debt-template"
  auth:
    type: bearer
    token: "{{Client_JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
