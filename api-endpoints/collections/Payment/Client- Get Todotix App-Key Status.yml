info:
  name: "Client: Get Todotix App-Key Status"
  type: http
  seq: 20

http:
  method: GET
  url: "{{API_PATH}}/api/payment/todotix-credential"
  auth:
    type: bearer
    token: "{{Client_JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
