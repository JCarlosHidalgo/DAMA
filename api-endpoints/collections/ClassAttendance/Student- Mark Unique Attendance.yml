info:
  name: "Student: Mark Unique Attendance"
  type: http
  seq: 9

http:
  method: POST
  url: "{{API_PATH}}/api/class-attendance/attendance/unique"
  body:
    type: json
    data: |-
      {
        "classId": "bec401bb-6be2-4d6b-b207-c6ad946a6b51",
        "courseName": "Curso de muestra"
      }
  auth:
    type: bearer
    token: "{{Student_JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
