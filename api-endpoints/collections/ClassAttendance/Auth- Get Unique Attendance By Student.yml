info:
  name: "Auth: Get Unique Attendance By Student"
  type: http
  seq: 5

http:
  method: GET
  url: "{{API_PATH}}/api/class-attendance/attendance/unique/student/00000000-0000-0000-0000-000000000000"
  auth:
    type: bearer
    token: "{{Client_JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
