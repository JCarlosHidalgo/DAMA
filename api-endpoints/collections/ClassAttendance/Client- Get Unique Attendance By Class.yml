info:
  name: "Client: Get Unique Attendance By Class"
  type: http
  seq: 4

http:
  method: GET
  url: "{{API_PATH}}/api/class-attendance/attendance/unique/class/00000000-0000-0000-0000-000000000000"
  auth:
    type: bearer
    token: "{{Client_JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
