info:
  name: "Student: List My Unique Attendance"
  type: http
  seq: 6

http:
  method: GET
  url: "{{API_PATH}}/api/class-attendance/attendance/unique/me?index=0"
  query:
    - name: index
      value: "0"
  auth:
    type: bearer
    token: "{{Student_JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
