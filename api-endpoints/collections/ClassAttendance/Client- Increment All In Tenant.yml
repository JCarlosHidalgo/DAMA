info:
  name: "Client: Increment All In Tenant"
  type: http
  seq: 12

http:
  method: POST
  url: "{{API_PATH}}/api/class-attendance/remain/client/tenant"
  body:
    type: json
    data: |-
      {
        "requestId": "7f9619ff-8b86-d011-b42d-00c04fc964ff",
        "quantity": 1
      }
  auth:
    type: bearer
    token: "{{Client_JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
