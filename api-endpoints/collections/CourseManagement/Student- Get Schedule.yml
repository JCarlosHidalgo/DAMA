info:
  name: "Student: Get Schedule"
  type: http
  seq: 27

http:
  method: GET
  url: "{{API_PATH}}/api/course-management/course/student/schedule?weekPaginationIndex=0"
  query:
    - name: weekPaginationIndex
      value: "0"
  auth:
    type: bearer
    token: "{{Student_JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
