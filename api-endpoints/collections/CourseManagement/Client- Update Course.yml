info:
  name: "Client: Update Course"
  type: http
  seq: 4

http:
  method: PUT
  url: "{{API_PATH}}/api/course-management/course/70d6b49b-7fa4-42cc-af3c-05f3f9c01de8"
  body:
    type: json
    data: |-
      {
        "name": "Heels Renombrado"
      }
  auth:
    type: bearer
    token: "{{Client_JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
