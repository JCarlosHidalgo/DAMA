info:
  name: "Client: Update Group"
  type: http
  seq: 22

http:
  method: PUT
  url: "{{API_PATH}}/api/course-management/course/group/1a111111-1111-4111-8111-111111111111"
  body:
    type: json
    data: |-
      {
        "name": "Mañanas (renombrado)"
      }
  auth:
    type: bearer
    token: "{{Client_JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
