info:
  name: "Teacher: List My Groups"
  type: http
  seq: 24

http:
  method: GET
  url: "{{API_PATH}}/api/course-management/course/group/teacher/me"
  auth:
    type: bearer
    token: "{{Teacher_JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
