info:
  name: "Client: Create Course"
  type: http
  seq: 3

http:
  method: POST
  url: "{{API_PATH}}/api/course-management/course"
  body:
    type: json
    data: |-
      {
        "name": "Curso Demo",
        "externalReference": "demo-001"
      }
  auth:
    type: bearer
    token: "{{Client_JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
