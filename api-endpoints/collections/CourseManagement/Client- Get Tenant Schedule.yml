info:
  name: "Client: Get Tenant Schedule"
  type: http
  seq: 7

http:
  method: GET
  url: "{{API_PATH}}/api/course-management/course/tenant/schedule?weekPaginationIndex=0"
  query:
    - name: weekPaginationIndex
      value: "0"
  auth:
    type: bearer
    token: "{{Client_JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
