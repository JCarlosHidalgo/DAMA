info:
  name: "Client: List Teachers"
  type: http
  seq: 6

http:
  method: GET
  url: "{{API_PATH}}/api/auth/teachers?pageIndex=0"
  query:
    - name: pageIndex
      value: "0"
  auth:
    type: bearer
    token: "{{Client_JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
