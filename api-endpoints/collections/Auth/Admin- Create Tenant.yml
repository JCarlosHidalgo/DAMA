info:
  name: "Admin: Create Tenant"
  type: http
  seq: 12

http:
  method: POST
  url: "{{API_PATH}}/api/auth/tenants"
  body:
    type: json
    data: |-
      {
        "name" : "Escuela Nueva"
      }
  auth:
    type: bearer
    token: "{{Admin_JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
