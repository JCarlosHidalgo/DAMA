info:
  name: "Public: Login Client"
  type: http
  seq: 1
  tags:
    - smoke-skip

http:
  method: POST
  url: "{{API_PATH}}/api/auth/login"
  body:
    type: json
    data: |-
      {
        "username" : "Client Example",
        "password" : "Admin123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
