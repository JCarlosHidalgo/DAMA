info:
  name: "Admin: Rename Tenant"
  type: http
  seq: 13

http:
  method: PUT
  url: "{{API_PATH}}/api/auth/tenants/c4c1c44d-7b5c-414a-b8df-a5537357ee30/name"
  body:
    type: json
    data: |-
      {
        "name" : "Escuela Example Renombrada"
      }
  auth:
    type: bearer
    token: "{{Admin_JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
