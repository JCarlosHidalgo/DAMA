info:
  name: "Public: Login Student"
  type: http
  seq: 11
  tags:
    - smoke-skip

http:
  method: POST
  url: "{{API_PATH}}/api/auth/login"
  body:
    type: json
    data: |-
      {
        "username" : "Student Example",
        "password" : "Admin123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
