info:
  name: "Client: Register Teacher"
  type: http
  seq: 2

http:
  method: POST
  url: "{{API_PATH}}/api/auth/register/teacher"
  body:
    type: json
    data: |-
      {
        "username" : "Teacher Nuevo",
        "password" : "Admin123"
      }
  auth:
    type: bearer
    token: "{{Client_JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
