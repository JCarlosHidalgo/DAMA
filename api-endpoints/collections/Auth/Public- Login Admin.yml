info:
  name: "Public: Login Admin"
  type: http
  seq: 3
  tags:
    - smoke-skip

http:
  method: POST
  url: "{{API_PATH}}/api/auth/login"
  body:
    type: json
    data: |-
      {
        "username" : "w7rudO521J2adG",
        "password" : "Admin123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
