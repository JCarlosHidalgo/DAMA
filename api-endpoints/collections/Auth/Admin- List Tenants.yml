info:
  name: "Admin: List Tenants"
  type: http
  seq: 11

http:
  method: GET
  url: "{{API_PATH}}/api/auth/tenants"
  auth:
    type: bearer
    token: "{{Admin_JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
