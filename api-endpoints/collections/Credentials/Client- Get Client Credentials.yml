info:
  name: "Client: Get Client Credentials"
  type: http
  seq: 1

http:
  method: GET
  url: "{{API_PATH}}/api/credentials/client-credentials"
  auth:
    type: bearer
    token: "{{Client_JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
