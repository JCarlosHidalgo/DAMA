info:
  name: "Student: Get Student Credentials"
  type: http
  seq: 3

http:
  method: GET
  url: "{{API_PATH}}/api/credentials/student-credentials"
  auth:
    type: bearer
    token: "{{Student_JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
