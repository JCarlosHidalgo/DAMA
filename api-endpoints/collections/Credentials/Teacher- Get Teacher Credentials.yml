info:
  name: "Teacher: Get Teacher Credentials"
  type: http
  seq: 2

http:
  method: GET
  url: "{{API_PATH}}/api/credentials/teacher-credentials"
  auth:
    type: bearer
    token: "{{Teacher_JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
