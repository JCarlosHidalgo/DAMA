# 3.3.1 Arquitectura

> ⬜ **Pendiente de redacción.** **Cubre:** IEEE 1016 (vista de arquitectura).
> **Guía:** [2-diseno-ieee-1016.md](../2-diseno-ieee-1016.md).

## Qué debe contener
- Justificación del patrón de **microservicios** detrás de un *api-gateway* nginx, con SPA Angular.
- Capas y límites de seguridad; bordes gRPC con TLS y mensajería Outbox + RabbitMQ.

## Diagramas (no dibujar UML a mano)
- **Vista de servicios (isométrica):** FossFlow [`../../fossflow/diagrams/dama-architecture.json`](../../fossflow/diagrams/dama-architecture.json).
- **Componentes/paquetes (backend):** grafo de directorios de **Doxygen** (`DIRECTORY_GRAPH`).
- **Módulos (frontend):** grafo de dependencias de **Compodoc**.

## Comandos de demostración (sugeridos)
```bash
sed -n '1,60p' infrastructure/docs/doxygen/Doxyfile          # config Doxygen (HAVE_DOT, UML_LOOK…)
cat infrastructure/docs/compodoc/tsconfig.doc.json            # alcance Compodoc del frontend
ls infrastructure/                                            # compose.dev/prod, gateway, environments
```
