# 3.3.7 Diagrama de despliegue

> ⬜ **Pendiente de redacción.** **Cubre:** IEEE 1016 (vista de despliegue).
> **Guía:** [2-diseno-ieee-1016.md](../2-diseno-ieee-1016.md).

## Qué debe contener
- Topología de despliegue: contenedores Docker, gateway nginx, MySQL por servicio, RabbitMQ,
  red Dokploy en producción y Cloudflare Tunnels para el TLS público.

## Diagramas (no dibujar UML a mano)
- **Vista de servicios/despliegue (isométrica):** FossFlow [`../../fossflow/diagrams/dama-architecture.json`](../../fossflow/diagrams/dama-architecture.json).

## Comandos de demostración (sugeridos)
```bash
ls infrastructure/compose.*.yaml                       # dev / prod / test
grep -n "container_name\|expose\|ports" infrastructure/compose.prod.yaml | head
```
